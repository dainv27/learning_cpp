## Pseudocode

```
Hàm SequentialSearch(mảng A, giá_trị cần tìm X):
    Cho i từ 0 đến độ dài của A - 1:
        Nếu A[i] == X:
            Trả về i  // Vị trí tìm thấy
    Kết thúc vòng lặp
    Trả về -1  // Không tìm thấy
```
